name: query_builder_mongo
description: MongoDB support for for package:query_builder.
version: 0.0.0
author: Tobe O <thosakwe@gmail.com>
environment:
  sdk: ">=1.19.0"
dependencies:
  mongo_dart: ^0.2.7
  query_builder: ^0.0.0
dependency_overrides:
  query_builder:
    path: ../query_builder